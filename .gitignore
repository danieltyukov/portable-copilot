# Portable runtime + per-drive state — never committed (weights, keys, sessions)
runtime/
data/
*.pyc
__pycache__/
.DS_Store

# Personal context dropped per-drive — never commit it (keep only the seed)
context/*
!context/README.txt
